for (var so1 = 9; so1 > 1; so1--) {
    for (var i = 1; i <= 10; i++) {
        var tich = so1 * i;
        document.write("<span>" + so1 + "</span> x  <span>" + i + "</span> = <span>" + tich + "</span><br>");
    }
    document.write("<br>");
}
